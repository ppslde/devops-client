namespace DevOps.Core
{
    public class Class1
    {
        public Class1()
        {
            //var devOpsApiClient = new DevOpsApiClient("https://dev.azure.com/{organization}/{project}/_apis/git/repositories", "{personalAccessToken}");

            //var pullRequests = await devOpsApiClient.GetPullRequestsAsync("{repositoryId}");

            //foreach (var pullRequest in pullRequests)
            //{
            //    Console.WriteLine($"Id: {pullRequest.Id}, Title: {pullRequest.Title}, Url: {pullRequest.Url}");
            //}
        }
    }
}